import React, {Component} from 'react';

class UserNotification extends Component {
  constructor(props) {
    super(props)
  }

  render () {
    console.log('Rendering <UserNotification />')

    return (
        <div className="user message system">
          <span className='user notification content'> const messages = this.state.messages.concat(data)
            this.setState({messages: messages})</span>
        </div>
      )
  }
}

export default UserNotification;
